info:
  name: Register User
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/users"
  body:
    type: json
    data: |-
      {
        "name": "Ian Roberts",
        "email": "testa@test.com",
        "password": "secure_passworD"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
